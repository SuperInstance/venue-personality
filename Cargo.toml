[package]
name = "venue-personality"
version = "0.1.0"
edition = "2021"
description = "How venues develop personality over time — the character of a place in the Grand Pattern"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
